\documentclass[10pt,conference,compsocconf]{IEEEtran}
\usepackage{style}
\usepackage{tgpagella}


\begin{document}
\title{
    A Comparative Analysis of Optimization Trajectories and Convergence Properties.
}
\author{
    Ozair Faizan \\
        \texttt{\href{mailto:ahmad.faizan@epfl.ch}{ahmad.faizan@epfl.ch}} \and
    Paul Tercier \\
        \texttt{\href{mailto:paul.tercier@epfl.ch}{paul.tercier@epfl.ch}}\\
}
\maketitle

% \setlength{\belowdisplayskip}{2pt} \setlength{\belowdisplayshortskip}{2pt}
% \setlength{\abovedisplayskip}{2pt} \setlength{\abovedisplayshortskip}{2pt}


\begin{abstract}
    The choice of the optimization algorithm determines training efficiency and generalization of
    deep neural networks. While methods such as Stochastic Gradient Descent and Adam have become
    field standards, recent works such as Lion \cite{chen2025lionsecretlysolvesconstrained}
    and Muon \cite{jordan2024muon} promise better convergence properties in practice.
    In this paper we present a comparative study of the optimization trajectories produced
    by Stochastic Gradient Descent, Adam, Lion and Muon applied to Multi-Layer
    Perceptron on the Fashion-MNIST dataset \cite{xiao2017/online}.
    To this end, we use a linear interpolation method: for parameters $\vec{\theta}_t$ before and
    $\vec{\theta}_{t+1}$ after an epoch, we evaluate the loss function at $N=50$ points along
    the connecting vector, allowing us to empirically verify convexity and smoothness properties
    along the trajectory.

    Furthermore, we evaluate the convergence points $\vec{\theta}$ by testing the condition:
    $$ f(\vec{\theta}) \leqslant f(\vec{x}) + \nabla f(\vec{\theta})\T (\vec{\theta} - \vec{x}), $$
    for points $\vec{x}$ within a neighborhood $B_\varepsilon(\vec{\theta})$. 

    We use constant learning rate, and batch-size found by grid-search and compare them with
    predicted theoretical values, all other hyperparameters are set to default.
\end{abstract}

\section{Introduction}
The choice of the optimizer is central to the process of training deep neural networks, 
influencing not only the speed of convergence but also the ability of the model to generalize. 
There is a vast number of available optimizers that can be applied to train 
a deep neural network, \cite{schneider2021descending} identified over one hundred unique algorithms, 
yet only two have dominated the scene: Stochastic Gradient Descent (SGD) \cite{robbins1951stochastic} and Adam \cite{kingma2015adam}. 
The Lion optimizer, which was developed based on symbolic search for update equations, 
is able to achieve similar levels of effectiveness without having problems with large amounts of memory usage. 
Furthermore, the Muon optimizer, developed initially to speed up the NanoGPT
training, has shown better convergence rates.

Empirical comparisons typically report aggregate metrics such as final test accuracy 
or training loss curves. Less studied is the geometry of the optimization trajectory 
itself: whether successive parameter updates traverse locally convex regions of the 
loss landscape, how smooth the loss appears along each step, and whether convergence 
points satisfy local optimality conditions. Our analysis shows behavioral differences between
optimizers that aggregate metrics alone may not reveal.

\section{Models and Methods}
We fit an MLP on the Fashion MNIST dataset which includes 10 different classes
for image classification. This dataset contains a total of 60,000 gray-scale images of size
$28\times28$. 
Our model is composed of two hidden layers of width 256 and 128 respectively, each with ReLU
activation, followed by a linear classification layer, giving a total of approximately 234K
trainable parameters.
The cross-entropy loss is used with no regularization nor weight-decay for simplicity.

The following four optimization algorithms are compared. Stochastic gradient descent updates its parameters using
\begin{equation*}
    \vec{\theta}_{t+1} = \vec{\theta}_t - \eta \vec{g}_t.
\end{equation*}
Here, $g_t$ represents the stochastic gradient. The Adam optimizer computes the first- and second-moment averages of the gradient and performs bias-corrected adaptive steps per coordinate. The Lion optimizer uses only the sign of a momentum-gradient sum as the update direction:
\begin{equation*}
    \vec{\theta}_{t+1} = \vec{\theta}_t -
                \eta \mathrm{sign}\bigl(\beta_1 \vec{m}_t + (1-\beta_1) \vec{g}_t\bigr).
\end{equation*}
The momentum is updated by
\begin{equation*}
    \vec{m}_{t+1} = \beta_2 \vec{m}_t + (1-\beta_2) \vec{g}_t.
\end{equation*}
The Muon optimizer implements the Nesterov-type momentum in the direction of steepest descent in terms of the spectral norm and approximates it by performing the Newton-Schulz orthogonalization of the gradient matrix.

The hyperparameters used for our analysis are found by performing a grid search over learning rates
in $\{10^{-4}, 10^{-3}, 10^{-2}\}$ and batch sizes in $\{32, 64, 128\}$ and selecting the
combination that activation the lowest training loss for each optimizers after epoch 20.
All other hyperparameters are kept at their published defaults.

For each optimizer, the parameter vectors $\vec{\theta}_t$ and $\vec{\theta}_{t+1}$ are stored at
each epoch for a total of 5 epochs.
The loss over the training set is computed at the points along the line
\begin{equation*}
    \vec{\theta}(\alpha) = (1-\alpha)\vec{\theta}_t + \alpha\vec{\theta}_{t+1}
\end{equation*}
for $\alpha \in \{0, 1/N, \ldots, 1\}$ and $N=50$.

This allows us to analyze local approximation of the loss along the ``average'' trajectory.

Furthermore, we verify the first-order characterization of convexity by sampling $K=50$ points in
the ball $B_\varepsilon(\vec{\theta})$ with $\varepsilon = 10^{-2}$, and checking,
$$ f(\vec{\theta}) \leqslant f(\vec{x}) + \nabla f(\vec{\theta})\T (\vec{\theta} - \vec{x}), $$
providing an empirical measure of local optimality.

\section{Results}
The selected learning rates and batch-sizes along with final train loss and test accuracy after 
20 epochs are shown in Table~\ref{tab:perf}.

\begin{table}[h]
\centering
\caption{Selected learning rates and final performance after 20 epochs.}
\label{tab:perf}
\begin{tabular}{lcccc}
\hline
Optimizer & LR & Batch Size & Train Loss & Test Acc. (\%) \\
\hline
SGD  & $10^{-2}$ & 32  & 0.2383 & 88.38 \\
Adam & $10^{-3}$ & 64  & 0.1426 & 88.66 \\
Lion & $10^{-4}$ & 128 & 0.1269 & 88.52 \\
Muon & $10^{-3}$ & 64  & 0.0600 & 90.01 \\
\hline
\end{tabular}
\end{table}

\begin{figure}[h]
    \centering
    \includegraphics[width=\columnwidth]{img/loss_trajectory_epoch1.pdf}
    \vspace{1ex}
    \includegraphics[width=0.48\columnwidth]{img/loss_trajectory_epoch2.pdf}
    \hfill
    \includegraphics[width=0.48\columnwidth]{img/loss_trajectory_epoch3.pdf}
    \vspace{1ex}
    \includegraphics[width=0.48\columnwidth]{img/loss_trajectory_epoch4.pdf}
    \hfill
    \includegraphics[width=0.48\columnwidth]{img/loss_trajectory_epoch5.pdf}
    \caption{Loss evaluated along the linear interpolation
    $\vec{\theta(\alpha)} = (1-\alpha)\vec{\theta}_t + \alpha\vec{\theta}_{t+1}$ for each epoch.
    Dashed lines show the linear interpolation of endpoint losses.}
    \label{fig:interp}
\end{figure}

{\color{magenta}TODO: Convergence test results.}


\section{Discussion}
{\color{magenta}TODO: Update for the new results.}

All four optimizers show competitive test accuracy 
on Fashion-MNIST dataset. The highest test accuracy is achieved by both Adam 87.74\% 
and Lion 87.81\%, followed by Muon with 87.29\% and SGD with 85.43\%. Lion optimizer 
achieves an accuracy comparable to Adam with a learning rate that is 10x lower than the 
latter due to the sign-based update producing larger step sizes.

{\color{magenta}TODO: Update for the new results.}
Loss Landscape along Update Directions.
Fig.~\ref{fig:interp} illustrates the evolution of loss values along $N=20$ interpolation points on the line
$\theta(\alpha) = (1 - \alpha)\theta_t + \alpha\theta_{t+1}$ across all 5 training epochs.
At the early stage of the training process (epoch 1), SGD and Muon produce non-convex paths,
while Adam and Lion are already showing convex paths. Starting from epoch 2, SGD, Adam,
and Lion demonstrate convex, monotonically decreasing paths, while Muon remains non-convex until epoch 4, 
reflecting the more exploratory nature of its orthogonalized updates.

{\color{magenta}TODO: Discuss the convergence test results.}


\section{Summary}
{\color{magenta}TODO: Repeat abstract with results in mind.}


\printbibliography

\end{document}


